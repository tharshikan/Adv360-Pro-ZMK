# Eager Debouncing: Maximum Snappiness
# Registers the key press IMMEDIATELY (0ms latency)
# Delays the release slightly (5ms) to prevent chatter
CONFIG_ZMK_KSCAN_DEBOUNCE_PRESS_MS=0
CONFIG_ZMK_KSCAN_DEBOUNCE_RELEASE_MS=5
